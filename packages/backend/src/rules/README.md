# rules/

Hard guardrail rules enforced by the Prompt Auditor Middleware (Story 13.4). Rules here are non-negotiable constraints (e.g., security policies, output format requirements) that override any agent or user instruction.
